{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "vercel-build": "composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader && npm run build",
        "dev": "vite"
    },
    "devDependencies": {
        "@tailwindcss/forms": "^0.5",
        "@tailwindcss/typography": "^0.5",
        "@tailwindcss/vite": "^4.1",
        "axios": "^1.12",
        "concurrently": "^9.2",
        "laravel-vite-plugin": "^2.0",
        "tailwindcss": "^4.1",
        "vite": "^7.1"
    }
}
